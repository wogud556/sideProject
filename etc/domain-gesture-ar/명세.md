가능해. 가장 단순한 MVP는 두 손의 21개 관절을 MediaPipe로 추적하고, 미리 정의한 손가락 자세가 일정 시간 유지되면 OpenCV로 영역 전개 이펙트를 웹캠 화면 위에 합성하는 방식이야.

MediaPipe Hand Landmarker는 손마다 21개 랜드마크와 왼손·오른손 정보를 반환하고, 연속 영상 입력도 처리할 수 있어 이런 프로젝트에 적합하다.  

아래 예제에서는 실제 작품 이미지를 사용하지 않고, 영역 전개에서 영감을 받은 오리지널 원형·입자 이펙트를 생성한다.

프로젝트: Domain Gesture AR

동작 방식

노트북 웹캠
    ↓
MediaPipe로 양손 추적
    ↓
손가락 펴짐/접힘 판정
    ↓
양손이 특정 자세로 0.8초 유지
    ↓
영역 전개 발동
    ↓
화면 어두워짐 + 마법진 + 충격파 + 입자 이펙트

예제 손동작은 다음과 같이 단순화한다.

양손 모두:
검지   : 펼침
중지   : 펼침
약지   : 접힘
새끼   : 접힘
두 손목이 화면 중앙에서 가까워짐

실제 작품 속 복잡한 손 모양을 완벽히 구분하려면 나중에 랜드마크 데이터를 직접 수집해 분류 모델을 학습하는 편이 좋다. 우선은 관절 좌표 규칙만으로 구현한다.

⸻

1. 프로젝트 구조

domain-gesture-ar/
├── main.py
└── requirements.txt

requirements.txt

opencv-python
mediapipe
numpy

설치:

python -m venv .venv
# macOS / Linux
source .venv/bin/activate
# Windows
# .venv\Scripts\activate
pip install -r requirements.txt

Python 3.10 또는 3.11 환경을 권장한다.

⸻

2. 전체 예제 코드

main.py

from __future__ import annotations
import math
import random
import time
from dataclasses import dataclass
import cv2
import mediapipe as mp
import numpy as np
WINDOW_NAME = "Domain Gesture AR"
HOLD_SECONDS = 0.8
EFFECT_SECONDS = 4.0
CAMERA_INDEX = 0
@dataclass
class Particle:
    angle: float
    speed: float
    radius: float
    size: int
class DomainEffect:
    """영역 전개풍 화면 효과를 관리한다."""
    def __init__(self) -> None:
        self.active = False
        self.started_at = 0.0
        self.particles: list[Particle] = []
    def start(self) -> None:
        self.active = True
        self.started_at = time.monotonic()
        self.particles = [
            Particle(
                angle=random.uniform(0, math.tau),
                speed=random.uniform(70, 260),
                radius=random.uniform(0, 80),
                size=random.randint(1, 5),
            )
            for _ in range(150)
        ]
    def stop(self) -> None:
        self.active = False
        self.particles.clear()
    def draw(self, frame: np.ndarray) -> np.ndarray:
        if not self.active:
            return frame
        elapsed = time.monotonic() - self.started_at
        if elapsed >= EFFECT_SECONDS:
            self.stop()
            return frame
        height, width = frame.shape[:2]
        center = (width // 2, height // 2)
        # 발동 초기 진행률
        intro_progress = min(elapsed / 0.8, 1.0)
        # 전체 화면을 어둡고 푸른 느낌으로 변경
        dark_layer = np.zeros_like(frame)
        dark_layer[:] = (45, 8, 20)
        frame = cv2.addWeighted(
            frame,
            0.42,
            dark_layer,
            0.58,
            0,
        )
        overlay = frame.copy()
        # 중앙에서 퍼져나가는 충격파
        shockwave_radius = int(intro_progress * max(width, height) * 0.75)
        cv2.circle(
            overlay,
            center,
            shockwave_radius,
            (255, 180, 80),
            thickness=4,
            lineType=cv2.LINE_AA,
        )
        cv2.circle(
            overlay,
            center,
            max(1, shockwave_radius - 15),
            (180, 60, 255),
            thickness=2,
            lineType=cv2.LINE_AA,
        )
        # 회전하는 마법진
        rotation = elapsed * 1.7
        for ring_index, base_radius in enumerate((90, 145, 210)):
            radius = int(base_radius * intro_progress)
            cv2.circle(
                overlay,
                center,
                radius,
                (255, 120 + ring_index * 30, 70),
                thickness=2,
                lineType=cv2.LINE_AA,
            )
            point_count = 8 + ring_index * 4
            for point_index in range(point_count):
                angle = (
                    math.tau * point_index / point_count
                    + rotation * (1 if ring_index % 2 == 0 else -1)
                )
                x = int(center[0] + math.cos(angle) * radius)
                y = int(center[1] + math.sin(angle) * radius)
                cv2.circle(
                    overlay,
                    (x, y),
                    3 + ring_index,
                    (255, 220, 170),
                    thickness=-1,
                    lineType=cv2.LINE_AA,
                )
        # 중앙 문양
        symbol_radius = int(60 * intro_progress)
        for index in range(6):
            angle = rotation + index * math.pi / 3
            start = (
                int(center[0] + math.cos(angle) * 15),
                int(center[1] + math.sin(angle) * 15),
            )
            end = (
                int(center[0] + math.cos(angle) * symbol_radius),
                int(center[1] + math.sin(angle) * symbol_radius),
            )
            cv2.line(
                overlay,
                start,
                end,
                (255, 210, 120),
                3,
                cv2.LINE_AA,
            )
        # 바깥쪽으로 퍼지는 입자
        for particle in self.particles:
            distance = particle.radius + particle.speed * elapsed
            x = int(center[0] + math.cos(particle.angle) * distance)
            y = int(center[1] + math.sin(particle.angle) * distance)
            if 0 <= x < width and 0 <= y < height:
                cv2.circle(
                    overlay,
                    (x, y),
                    particle.size,
                    (255, random.randint(100, 220), 80),
                    thickness=-1,
                    lineType=cv2.LINE_AA,
                )
        # 화면 가장자리 왜곡처럼 보이는 선
        for index in range(18):
            angle = math.tau * index / 18 + rotation * 0.3
            outer_radius = int(max(width, height) * 0.7)
            inner_radius = int(240 + 15 * math.sin(elapsed * 5 + index))
            start = (
                int(center[0] + math.cos(angle) * inner_radius),
                int(center[1] + math.sin(angle) * inner_radius),
            )
            end = (
                int(center[0] + math.cos(angle) * outer_radius),
                int(center[1] + math.sin(angle) * outer_radius),
            )
            cv2.line(
                overlay,
                start,
                end,
                (130, 50, 200),
                1,
                cv2.LINE_AA,
            )
        frame = cv2.addWeighted(frame, 0.4, overlay, 0.8, 0)
        self._draw_title(frame, elapsed)
        return frame
    @staticmethod
    def _draw_title(frame: np.ndarray, elapsed: float) -> None:
        height, width = frame.shape[:2]
        # 발동 후 잠깐 뒤에 문구 표시
        if elapsed < 0.35:
            return
        title = "DOMAIN EXPANSION"
        subtitle = "VOID OF RESONANCE"
        title_scale = min(width / 900, 1.2)
        title_size = cv2.getTextSize(
            title,
            cv2.FONT_HERSHEY_DUPLEX,
            title_scale,
            2,
        )[0]
        title_x = (width - title_size[0]) // 2
        title_y = height - 95
        # 그림자
        cv2.putText(
            frame,
            title,
            (title_x + 3, title_y + 3),
            cv2.FONT_HERSHEY_DUPLEX,
            title_scale,
            (0, 0, 0),
            5,
            cv2.LINE_AA,
        )
        cv2.putText(
            frame,
            title,
            (title_x, title_y),
            cv2.FONT_HERSHEY_DUPLEX,
            title_scale,
            (240, 220, 255),
            2,
            cv2.LINE_AA,
        )
        subtitle_size = cv2.getTextSize(
            subtitle,
            cv2.FONT_HERSHEY_SIMPLEX,
            0.65,
            1,
        )[0]
        subtitle_x = (width - subtitle_size[0]) // 2
        cv2.putText(
            frame,
            subtitle,
            (subtitle_x, title_y + 35),
            cv2.FONT_HERSHEY_SIMPLEX,
            0.65,
            (220, 170, 255),
            1,
            cv2.LINE_AA,
        )
def distance(point_a, point_b) -> float:
    return math.hypot(
        point_a.x - point_b.x,
        point_a.y - point_b.y,
    )
def finger_is_extended(
    landmarks,
    tip_index: int,
    pip_index: int,
    mcp_index: int,
) -> bool:
    """
    손목에서 손가락 끝까지의 거리가
    손목에서 중간 관절까지의 거리보다 충분히 길면 펼쳐진 것으로 판단한다.
    손 회전에도 비교적 대응하기 위해 단순 y좌표가 아닌 거리 기반으로 판정한다.
    """
    wrist = landmarks[0]
    tip_distance = distance(wrist, landmarks[tip_index])
    pip_distance = distance(wrist, landmarks[pip_index])
    mcp_distance = distance(wrist, landmarks[mcp_index])
    return (
        tip_distance > pip_distance * 1.08
        and tip_distance > mcp_distance * 1.25
    )
def matches_domain_gesture(landmarks) -> bool:
    """
    예제 영역 전개 자세:
    검지와 중지는 펼치고,
    약지와 새끼손가락은 접는다.
    엄지는 이번 MVP 판정에서 제외한다.
    """
    index_extended = finger_is_extended(
        landmarks,
        tip_index=8,
        pip_index=6,
        mcp_index=5,
    )
    middle_extended = finger_is_extended(
        landmarks,
        tip_index=12,
        pip_index=10,
        mcp_index=9,
    )
    ring_extended = finger_is_extended(
        landmarks,
        tip_index=16,
        pip_index=14,
        mcp_index=13,
    )
    pinky_extended = finger_is_extended(
        landmarks,
        tip_index=20,
        pip_index=18,
        mcp_index=17,
    )
    return (
        index_extended
        and middle_extended
        and not ring_extended
        and not pinky_extended
    )
def hands_are_close(first_hand, second_hand) -> bool:
    """
    두 손의 손목과 검지 시작점이 가까운지 확인한다.
    좌표가 0~1로 정규화돼 있으므로 화면 크기와 무관하게 사용할 수 있다.
    """
    wrist_distance = distance(first_hand[0], second_hand[0])
    index_base_distance = distance(first_hand[5], second_hand[5])
    return wrist_distance < 0.38 and index_base_distance < 0.30
def draw_activation_meter(
    frame: np.ndarray,
    progress: float,
    gesture_detected: bool,
) -> None:
    height, width = frame.shape[:2]
    bar_width = min(360, width - 80)
    bar_height = 18
    x = (width - bar_width) // 2
    y = height - 45
    cv2.rectangle(
        frame,
        (x, y),
        (x + bar_width, y + bar_height),
        (40, 40, 40),
        thickness=-1,
    )
    filled_width = int(bar_width * progress)
    cv2.rectangle(
        frame,
        (x, y),
        (x + filled_width, y + bar_height),
        (220, 120, 255),
        thickness=-1,
    )
    border_color = (255, 255, 255) if gesture_detected else (130, 130, 130)
    cv2.rectangle(
        frame,
        (x, y),
        (x + bar_width, y + bar_height),
        border_color,
        thickness=2,
    )
    status = "GESTURE LOCKED" if gesture_detected else "SHOW BOTH HANDS"
    cv2.putText(
        frame,
        status,
        (x, y - 10),
        cv2.FONT_HERSHEY_SIMPLEX,
        0.55,
        border_color,
        1,
        cv2.LINE_AA,
    )
def main() -> None:
    capture = cv2.VideoCapture(CAMERA_INDEX)
    if not capture.isOpened():
        raise RuntimeError(
            "웹캠을 열 수 없습니다. CAMERA_INDEX 또는 카메라 권한을 확인하세요."
        )
    capture.set(cv2.CAP_PROP_FRAME_WIDTH, 1280)
    capture.set(cv2.CAP_PROP_FRAME_HEIGHT, 720)
    mp_hands = mp.solutions.hands
    mp_drawing = mp.solutions.drawing_utils
    mp_styles = mp.solutions.drawing_styles
    effect = DomainEffect()
    gesture_started_at: float | None = None
    trigger_locked = False
    with mp_hands.Hands(
        static_image_mode=False,
        max_num_hands=2,
        model_complexity=1,
        min_detection_confidence=0.65,
        min_tracking_confidence=0.65,
    ) as hands:
        while True:
            success, frame = capture.read()
            if not success:
                print("웹캠 프레임을 읽지 못했습니다.")
                break
            # 거울처럼 보이도록 좌우 반전
            frame = cv2.flip(frame, 1)
            rgb_frame = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)
            rgb_frame.flags.writeable = False
            result = hands.process(rgb_frame)
            rgb_frame.flags.writeable = True
            detected_hands = []
            if result.multi_hand_landmarks:
                for hand_landmarks in result.multi_hand_landmarks:
                    detected_hands.append(hand_landmarks.landmark)
                    mp_drawing.draw_landmarks(
                        frame,
                        hand_landmarks,
                        mp_hands.HAND_CONNECTIONS,
                        mp_styles.get_default_hand_landmarks_style(),
                        mp_styles.get_default_hand_connections_style(),
                    )
            gesture_detected = False
            if len(detected_hands) == 2:
                first_matches = matches_domain_gesture(detected_hands[0])
                second_matches = matches_domain_gesture(detected_hands[1])
                close_enough = hands_are_close(
                    detected_hands[0],
                    detected_hands[1],
                )
                gesture_detected = (
                    first_matches
                    and second_matches
                    and close_enough
                )
            now = time.monotonic()
            hold_progress = 0.0
            if gesture_detected and not effect.active:
                if gesture_started_at is None:
                    gesture_started_at = now
                held_seconds = now - gesture_started_at
                hold_progress = min(held_seconds / HOLD_SECONDS, 1.0)
                if held_seconds >= HOLD_SECONDS and not trigger_locked:
                    effect.start()
                    trigger_locked = True
            else:
                gesture_started_at = None
                # 손동작을 해제해야 다시 발동할 수 있도록 잠금 해제
                if not gesture_detected:
                    trigger_locked = False
            if effect.active:
                frame = effect.draw(frame)
            else:
                draw_activation_meter(
                    frame,
                    hold_progress,
                    gesture_detected,
                )
            cv2.putText(
                frame,
                "Q: quit | R: manual activation",
                (20, 32),
                cv2.FONT_HERSHEY_SIMPLEX,
                0.65,
                (255, 255, 255),
                1,
                cv2.LINE_AA,
            )
            cv2.imshow(WINDOW_NAME, frame)
            key = cv2.waitKey(1) & 0xFF
            if key == ord("q"):
                break
            if key == ord("r"):
                effect.start()
    capture.release()
    cv2.destroyAllWindows()
if __name__ == "__main__":
    main()

OpenCV의 VideoCapture는 카메라 장치에서 프레임을 연속으로 읽는 역할을 한다. 카메라 번호 0은 일반적으로 기본 웹캠이며, 종료 시에는 release()로 장치를 반환해야 한다.  

⸻

3. 실행

python main.py

실행 후 다음 순서로 테스트한다.

1. 웹캠 앞에 양손을 올린다.
2. 양손에서 검지와 중지만 편다.
3. 약지와 새끼손가락은 접는다.
4. 두 손을 화면 중앙에서 가까이 모은다.
5. 자세를 약 0.8초 유지한다.
6. 화면에 영역 이펙트가 약 4초 동안 표시된다.
7. Q를 누르면 종료되고 R을 누르면 인식 없이 이펙트를 테스트할 수 있다.

⸻

4. 현재 예제의 핵심 로직

손가락 끝과 관절의 MediaPipe 인덱스는 다음과 같다.

손목:       0
검지:
MCP         5
PIP         6
끝          8
중지:
MCP         9
PIP        10
끝         12
약지:
MCP        13
PIP        14
끝         16
새끼:
MCP        17
PIP        18
끝         20

MediaPipe가 손 하나당 21개 랜드마크를 제공하기 때문에 각 손가락 끝과 중간 관절의 위치 관계로 손가락이 펴졌는지를 계산할 수 있다.  

index_extended = finger_is_extended(
    landmarks,
    tip_index=8,
    pip_index=6,
    mcp_index=5,
)

그리고 양손이 모두 지정된 패턴인지 검사한다.

gesture_detected = (
    first_matches
    and second_matches
    and close_enough
)

한 프레임에서만 손 모양이 감지됐다고 바로 발동하지 않고 0.8초 동안 유지하게 만들어 오작동을 줄였다.

held_seconds = now - gesture_started_at
if held_seconds >= 0.8:
    effect.start()

⸻

5. 다른 영역 동작 추가 방식

각 영역마다 별도 판정 함수를 만들면 된다.

def matches_gesture_a(landmarks) -> bool:
    # 검지만 펼침
    return (
        finger_is_extended(landmarks, 8, 6, 5)
        and not finger_is_extended(landmarks, 12, 10, 9)
        and not finger_is_extended(landmarks, 16, 14, 13)
        and not finger_is_extended(landmarks, 20, 18, 17)
    )
def matches_gesture_b(landmarks) -> bool:
    # 검지, 중지, 약지 펼침
    return (
        finger_is_extended(landmarks, 8, 6, 5)
        and finger_is_extended(landmarks, 12, 10, 9)
        and finger_is_extended(landmarks, 16, 14, 13)
        and not finger_is_extended(landmarks, 20, 18, 17)
    )

판정 결과에 따라 다른 이펙트를 호출한다.

if gesture_a:
    blue_void_effect.start()
elif gesture_b:
    flame_shrine_effect.start()
elif gesture_c:
    shadow_garden_effect.start()

추천 매핑은 다음 정도다.

손동작	오리지널 효과
검지·중지 펼침	푸른 무한 공간
손바닥 교차	붉은 신전
양손을 아래로 펼침	검은 그림자 정원
손가락으로 원 구성	황금빛 심판 공간

⸻

6. 정확하게 작품 속 손 모양을 인식하려면

현재 코드는 “어떤 손가락이 펴졌는가”만 확인하기 때문에 손가락 교차, 관절 방향, 양손의 앞뒤 관계까지 정확히 구분하지는 못한다.

정확도를 높이려면 다음 단계가 필요하다.

각 영역 손동작 영상 촬영
    ↓
MediaPipe 랜드마크 42개 추출
    ↓
손목 기준으로 좌표 정규화
    ↓
Gesture 클래스별 데이터 저장
    ↓
RandomForest 또는 작은 신경망 학습
    ↓
실시간 분류 확률 계산
    ↓
90% 이상일 때 효과 실행

입력 데이터는 양손 기준으로 대략 다음 형태가 된다.

왼손  21개 × x,y,z
오른손 21개 × x,y,z
총 126개 숫자

정적인 손동작은 RandomForest, SVM 같은 모델로도 시작할 수 있고, 손동작 과정까지 구분하려면 최근 20~30프레임을 입력받는 LSTM이나 Transformer 방식으로 확장할 수 있다. 단순 랜드마크 규칙보다 별도의 제스처 분류기를 사용하는 편이 다양한 조명과 촬영 조건에서 더 견고할 수 있다.  

이 MVP만으로도 손동작 감지 → 유지 시간 검증 → 영역 이펙트 발동 → 재발동 잠금 처리까지 전체 흐름을 직접 실행해볼 수 있다.


cd domain-gesture-ar
source .venv/bin/activate   # 이미 만들어둠, 재설치 불필요
python main.py