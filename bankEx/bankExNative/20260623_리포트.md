작성하겠습니다.
---
BankEx Android Native 개발 리포트

1. 프로젝트 개요
BankEx Android Native 개발 리포트

1. 프로젝트 개요

┌────────────────────────┬──────────────────────────────────────────────────────────────────┐
│          항목          │                                     │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ 목표                   │ BankEx 웹 서비스를  안정적으로 실행 │
내용 전부 확인했습니다. 전체 개발 리포트 작성합니다.

---
BankEx Android Native 개발 리포트

1. 프로젝트 개요

┌────────────────────────┬──────────────────────────────────────────────────────────────────┐
│          항목          │                                     │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ 목표                   │ BankEx 웹 서비스를 Android Native WebView 앱으로 안정적으로 실행 │
├────────────────────────┼─────────────────────────────────────┤
│ 언어                   │ Kotlin 2.2.10                                                    │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ UI 프레임워크          │ Jetpack Compose + AndroidView                                    │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ compileSdk / targetSdk │ 37                                  │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ minSdk                 │ 27 (Android 8.1 Ore                 │
├────────────────────────┼──────────────────────────────────────────────────────────────────┤
│ 접근 URL               │ http://10.0.2.2:517                 │
└────────────────────────┴──────────────────────────────────────────────────────────────────┘

---
2. 전체 파일 구조

bankExNative/
├── app/src/main/
│   ├── AndroidManifest.xml                        ← 권한, 보안 설정
│   ├── res/xml/network_security_config.xml   0.0.2.2만)
│   └── java/com/hanati/bankexnative/
│       ├── MainActivity.kt                        ← 진입점 (얇게 유지)
│       ├── config/
│       │   └── WebConfig.kt                       ← URL, UserAgent 상수
│       ├── webview/
│       │   ├── WebViewConfigurator.kt             ← WebSettings 설정
│       │   ├── AppWebViewClient.kt
│       │   └── AppWebChromeClient.kt              ← 로딩 진행률
│       ├── network/
│       │   └── NetworkChecker.kt
│       └── ui/
│           ├── WebViewScreen.kt                   ← 메인 화면 (Compose)
│           └── ErrorScreen.kt                     ← 에러/재시도 화면
└── app/src/test/java/com/hanati/bankexnative/
    ├── WebViewConfiguratorTest.kt            ic)
    ├── AppWebViewClientTest.kt                    ← 에러 처리 검증 (Robolectric + MockK)
    ├── WebViewUrlTest.kt
    ├── BackNavigationTest.kt                      ← 뒤로가기 분기 검증 (MockK)
    └── NetworkCheckerTest.kt                 ckK)

---
3. 구현 소스코드

AndroidManifest.xml — 권한 및 보안 설정

<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<application
    android:networkSecurityConfig="@xml/network_security_config"
    ...>

network_security_config.xml — HTTP 허용 범위 제한

전체 HTTP를 여는 대신 개발 도메인만 허용합니다.

<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>

---
config/WebConfig.kt — 상수 관리

object WebConfig {
    const val BASE_URL = "http://10.0.2.2:5173
    const val USER_AGENT = "BankExNative/1.0 Android"
}

---
webview/WebViewConfigurator.kt — WebSettings

Activity에서 설정 로직을 분리해 테스트 가능하

class WebViewConfigurator {
    fun configure(settings: WebSettings) {
        settings.javaScriptEnabled = true               // JS 활성화
        settings.domStorageEnabled = true               // DOM Storage 활성화
        settings.userAgentString = WebConfig.USER_AGENT // UA 설정
        settings.mixedContentMode =
            WebSettings.MIXED_CONTENT_COMPATIB 허용
        settings.useWideViewPort = true                 // 뷰포트 최적화
        settings.loadWithOverviewMode = true
    }
}

---
webview/AppWebViewClient.kt — 에러 처리

메인 프레임 에러만 화면에 표시하고, 서브 리소  합니다.

class AppWebViewClient(
    private val onPageStarted: () -> Unit,
    private val onPageFinished: (canGoBack: Boolean) -> Unit,
    private val onError: (String) -> Unit
) : WebViewClient() {

    override fun onPageStarted(view: WebView, url: String, favicon: Bitmap?) {
        super.onPageStarted(view, url, favicon
        onPageStarted()
    }

    override fun onPageFinished(view: WebView,
        super.onPageFinished(view, url)
        onPageFinished(view.canGoBack())   // 뒤로가기 가능 여부도 함께 전달
    }

    override fun onReceivedError(view: WebView, request: WebResourceRequest, error: WebResourceError) {
        if (request.isForMainFrame) {      //
            onError("페이지를 불러올 수 없습니다. (${error.errorCode})")
        }
    }

    override fun onReceivedHttpError(view: WebView, request: WebResourceRequest, errorResponse: WebResourceResponse) {
        if (request.isForMainFrame) {
            onError("서버 오류가 발생했습니다. (HTTP ${errorResponse.statusCode})")
        }
    }

    override fun onReceivedSslError(view: WebView, handler: SslErrorHandler, error: SslError) {
        handler.cancel()                   // SSL 오류 시 보안상 취소
        onError("SSL 인증서 오류가 발생했습니
    }
}

---
webview/AppWebChromeClient.kt — 로딩 진행률

class AppWebChromeClient(
    private val onProgressChanged: (Int) -> Unit
) : WebChromeClient() {

    override fun onProgressChanged(view: WebView, newProgress: Int) {
        super.onProgressChanged(view, newProgr
        onProgressChanged(newProgress)
    }
}

---
network/NetworkChecker.kt — 네트워크 확인

인터페이스로 분리해 테스트 시 MockK로 대체할

interface NetworkChecker {
    fun isConnected(): Boolean
}

class ConnectivityNetworkChecker(private val context: Context) : NetworkChecker {
    override fun isConnected(): Boolean {
        val cm = context.getSystemService(Context.CONNECTIVITY_SERVICE) as ConnectivityManager
        val network = cm.activeNetwork ?: return false
        val caps = cm.getNetworkCapabilities(network) ?: return false
        return caps.hasCapability(NetworkCapabilities.NET_CAPABILITY_INTERNET)
    }
}

---
ui/WebViewScreen.kt — 메인 화면

이 파일이 모든 흐름을 조합하는 핵심입니다.

@Composable
fun WebViewScreen(url: String = WebConfig.BASE_URL) {
    var isLoading by remember { mutableStateOf
    var errorMessage by remember { mutableStateOf<String?>(null) }
    var canGoBack by remember { mutableStateOf(false) }
    var webViewRef by remember { mutableStateO

    // canGoBack = true일 때만 BackHandler가 이벤트 소비
    // false이면 시스템이 이벤트를 받아 Activi
    BackHandler(enabled = canGoBack) {
        webViewRef?.goBack()
    }

    if (errorMessage != null) {
        ErrorScreen(
            message = errorMessage!!,
            onRetry = {
                errorMessage = null
                isLoading = true
                webViewRef?.loadUrl(url)
            }
        )
        return
    }

    Box(modifier = Modifier.fillMaxSize()) {
        AndroidView(
            factory = { context ->
                WebView(context).also { webView ->
                    webViewRef = webView
                    WebViewConfigurator().conf
                    webView.webViewClient = AppWebViewClient(
                        onPageStarted = { isLoading = true },
                        onPageFinished = { new
                            isLoading = false
                            canGoBack = newCanGoBack
                        },
                        onError = { msg ->
                            isLoading = false
                            errorMessage = msg
                        }
                    )
                    webView.webChromeClient = AppWebChromeClient(
                        onProgressChanged = { ess < 100 }
                    )
                    webView.loadUrl(url)
                }
            },
            modifier = Modifier.fillMaxSize()
        )

        if (isLoading) {
            CircularProgressIndicator(modifier = Modifier.align(Alignment.Center))
        }
    }
}

---
ui/ErrorScreen.kt — 에러/재시도 화면

@Composable
fun ErrorScreen(message: String, onRetry: () -> Unit) {
    Column(
        modifier = Modifier.fillMaxSize(),
        verticalArrangement = Arrangement.Center,
        horizontalAlignment = Alignment.CenterHorizontally
    ) {
        Text(text = message, style = MaterialTheme.typography.bodyLarge)
        Spacer(modifier = Modifier.height(16.d
        Button(onClick = onRetry) { Text("다시 시도") }
    }
}

---
MainActivity.kt — 진입점

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState:
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            BankExNativeTheme {
                WebViewScreen()
            }
        }
    }
}

---
4. 테스트 코드 요약

┌─────────────────────────┬──────────────────┬───────────────────────────────────────────┐
│          파일           │       도구       │ 테스트 케이스 │                       검증 내용                        │
│                         │                  │                                           │
├─────────────────────────┼──────────────────┼───────────────┼────────────────────────────────────────────────────────┤
│ WebViewConfiguratorTest │ Robolectric      │M Storage, UA, MixedContent, WideViewPort  │
├─────────────────────────┼──────────────────┼───────────────┼────────────────────────────────────────────────────────┤
│ AppWebViewClientTest    │ Robolectric +    │ 료 콜백, 메인프레임에러, 서브프레임무시,  │
│                         │ MockK            │               │ HTTP에러, SSL에러                                      │
├─────────────────────────┼──────────────────┼───────────────────────────────────────────┤
│ WebViewUrlTest          │ MockK            │      5개      │ BASE_URL 형식, loadUrl 호출, 빈URL/잘못된URL 에러처리  │
├─────────────────────────┼──────────────────┼───────────────────────────────────────────┤
│ BackNavigationTest      │ MockK            │      3개      │ goBack 호출, goBack 미호출, canGoBack 상태 업데이트    │
├─────────────────────────┼──────────────────┼───────────────────────────────────────────┤
│ NetworkCheckerTest      │ MockK            │      3개      │ 연결됨/끊김 반환값, 미연결 시 에러 유발                │
├─────────────────────────┼──────────────────┼───────────────────────────────────────────┤
│ 합계                    │                  │     22개      │                                                        │
└─────────────────────────┴──────────────────┴───────────────────────────────────────────┘

▎ 마지막 테스트 실행 결과: 23 tests completed, 람다 mock 이슈 — 수정 완료, 재실행 필요)

---
5. 실행 방법

사전 준비

BankEx 프론트엔드 서버를 먼저 실행해야 합니다.

# 터미널 1 — 프론트엔드 서버 실행
cd loan/front/bankEx_Front
npm run dev
# → http://localhost:5173 에서 실행됨

앱 실행 (에뮬레이터)

# Android Studio에서 에뮬레이터를 먼저 켠 후
cd bankExNative
./gradlew installDebug    # 에뮬레이터에 앱 설
# 또는 Android Studio에서 Run 버튼 클릭

앱이 실행되면 에뮬레이터는 10.0.2.2:5173으로 host PC의 Vite 서버에 접근합니다.

앱 실행 (실제 기기)

// WebConfig.kt의 BASE_URL을 PC의 LAN IP로 변경
const val BASE_URL = "http://192.168.x.x:5173"

# Vite도 외부 접근 허용 모드로 실행
npm run dev -- --host

테스트 실행

cd bankExNative

# 전체 테스트
./gradlew test

# 특정 테스트만
./gradlew test --tests "*.WebViewConfiguratorTest"
./gradlew test --tests "*.AppWebViewClientTest
./gradlew test --tests "*.BackNavigationTest"

# 결과 HTML 리포트 확인
open app/build/reports/tests/testDebugUnitTest

---
6. 다음 세션에서 할 것

- ./gradlew test 재실행으로 전체 22개 통과 확인 (수정 내용 반영)
- 에뮬레이터에서 실제 앱 동작 확인 (로딩 스피너 → BankEx 화면 렌더링)
