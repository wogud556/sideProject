spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:bankex;DB_CLOSE_DELAY=-1
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
