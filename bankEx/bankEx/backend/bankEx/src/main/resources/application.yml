spring:
  application:
    name: bankEx
  profiles:
    active: local
server:
  port: 8080
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
